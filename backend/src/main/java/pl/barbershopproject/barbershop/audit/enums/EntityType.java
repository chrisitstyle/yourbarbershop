package pl.barbershopproject.barbershop.audit.enums;

/**
 * enum representing domain entity types tracked in audit logs
 */
public enum EntityType {
    ORDER,
    GUEST_ORDER,
    OFFER,
    USER
}
